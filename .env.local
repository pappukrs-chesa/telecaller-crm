# Local development overrides. NOT committed.
#
# Points the dashboard at the standalone telecaller_api_gateway running on
# :4002 while we're separate from chesa_api_gateway. After the merge, drop
# this file and let lib/api-config.ts use its prod default.
NEXT_PUBLIC_API_BASE_URL=http://localhost:4002
NEXT_PUBLIC_APP_ENV=dev
